% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.R
\name{cjk_summary}
\alias{cjk_summary}
\title{Summarise CJK content in a text column}
\usage{
cjk_summary(data, col)
}
\arguments{
\item{data}{A data frame or tibble containing a text column.}

\item{col}{The text column to scan, supplied unquoted. A non-character
column is coerced with \code{\link[=as.character]{as.character()}}; see \code{\link[=has_cjk]{has_cjk()}} for why
that makes a numeric column a poor thing to measure.}
}
\value{
A one-row tibble with columns \code{n_docs} (all entries), \code{n_with_cjk}
(entries holding at least one CJK character), \code{prop_with_cjk} and
\code{mean_ratio}. \code{prop_with_cjk} is \code{NA} for a zero-row column, and
\code{mean_ratio} is \code{NA} when no entry has a ratio to contribute.
}
\description{
\code{cjk_summary()} reports how much of a text column is CJK: how many entries
contain any CJK at all, what share of entries that is, and the mean share of
each entry's characters that are CJK.
}
\details{
\code{prop_with_cjk} counts an entry once however much CJK it holds, so it
answers "how many of these documents are CJK at all". \code{mean_ratio} averages
\code{\link[=cjk_ratio]{cjk_ratio()}} over the entries that have one, so it answers "how CJK are
they". A corpus of English with one ideograph per row scores high on the
first and near zero on the second.

\code{NA} entries never count as containing CJK, and they do count towards
\code{n_docs} -- so they are in the denominator of \code{prop_with_cjk} and dilute it:
a column that is half missing cannot score above 0.5. \code{mean_ratio} is the
one figure they are dropped from, along with empty strings, because neither
has a ratio to contribute.

Grouping is ignored: the result is always one row for the whole column. Use
\code{\link[dplyr:group_modify]{dplyr::group_modify()}} if you need it per group.
}
\examples{
df <- data.frame(
  text = c("\u4e2d\u6587", "mixed \u4e2d\u6587 text", "plain ASCII", NA)
)
cjk_summary(df, text)
}
\seealso{
\code{\link[=cjk_char_counts]{cjk_char_counts()}} for the per-character breakdown, \code{\link[=cjk_ratio]{cjk_ratio()}}
for the per-row measure this averages.
}
