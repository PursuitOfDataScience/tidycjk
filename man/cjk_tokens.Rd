% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment.R
\name{cjk_tokens}
\alias{cjk_tokens}
\title{One row per token}
\usage{
cjk_tokens(data, col, engine, ...)
}
\arguments{
\item{data}{A data frame or tibble containing a text column.}

\item{col}{The text column to scan, supplied unquoted.}

\item{engine}{Name of a segmentation engine, or a function implementing
one. Required; see \code{\link[=cjk_segmenters]{cjk_segmenters()}}.}

\item{...}{Passed to the engine.}
}
\value{
\code{data}, as a tibble, with one row per token and an added \code{token}
column. Row order follows the input, and tokens within a row follow the
text.
}
\description{
\code{cjk_tokens()} segments a text column and returns one row per token,
carrying the other columns along. It is the CJK-aware counterpart of
\href{https://CRAN.R-project.org/package=tidytext}{tidytext}'s
\code{unnest_tokens()}, which splits on whitespace and therefore
returns CJK sentences whole.
}
\details{
Rows that produce no tokens -- empty strings, and text with nothing an
engine recognises -- are dropped, as they are in \pkg{tidytext}. \code{NA} text
yields one row with an \code{NA} token, so a missing document does not silently
vanish from the output.

The token column is called \code{token} and is added to \code{data}; an existing
column of that name is replaced. As with \code{\link[=cjk_segment]{cjk_segment()}}, \code{engine} is
required.

Grouping is dropped, as it is by \code{\link[=cjk_summary]{cjk_summary()}}: the result is a plain
tibble even when \code{data} is a \code{grouped_df}. Regroup it afterwards if you need
the groups back.
}
\examples{
df <- data.frame(
  id = 1:2,
  text = c("\u6211\u5f88\u958b\u5fc3", "hello \u4e2d\u6587")
)
cjk_tokens(df, text, engine = "character")
}
\seealso{
\code{\link[=cjk_segment]{cjk_segment()}} for the vector version, \code{\link[=cjk_char_counts]{cjk_char_counts()}} when
you want characters rather than words.
}
