% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect.R
\name{cjk_ratio}
\alias{cjk_ratio}
\title{What share of the text is CJK?}
\usage{
cjk_ratio(x)
}
\arguments{
\item{x}{A character vector. Anything else is coerced with \code{\link[=as.character]{as.character()}}.}
}
\value{
A numeric vector the same length as \code{x}, between 0 and 1. \code{NA} input
gives \code{NA}. The empty string gives \code{NA} rather than 0, because the ratio is
0/0 and undefined.
}
\description{
\code{cjk_ratio()} reports the proportion of each string's characters that fall
in a CJK Unicode block, from 0 to 1. It is the natural way to find the rows
of a mixed corpus that are actually CJK, as opposed to the ones carrying a
single stray ideograph.
}
\details{
Characters are counted as Unicode code points, so an ideograph from a
supplementary plane counts once, not twice. The denominator is every
character in the string, including spaces and Latin punctuation.
}
\examples{
cjk_ratio(c("\u4e2d\u6587", "half \u4e2d\u6587", "none", "", NA))
}
\seealso{
\code{\link[=has_cjk]{has_cjk()}} for the yes/no version, \code{\link[=cjk_summary]{cjk_summary()}} for the
column-level summary.
}
