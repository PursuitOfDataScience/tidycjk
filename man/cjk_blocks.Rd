% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ranges.R
\name{cjk_blocks}
\alias{cjk_blocks}
\title{The Unicode blocks 'tidycjk' recognises}
\usage{
cjk_blocks()
}
\value{
A tibble with one row per block and columns \code{block} (the Unicode
block name), \code{script} (tidycjk's script label), \code{start} and \code{end} (the
inclusive code point bounds, as integers) and \code{n_codepoints}.
}
\description{
\code{cjk_blocks()} returns the block table that every other function in the
package consults. It is the package's definition of "CJK", written down and
exported so that it can be inspected rather than guessed at.
}
\details{
The table is sorted by \code{start} and contains no overlapping ranges, which is
what allows a whole corpus of code points to be classified with a single
\code{\link[=findInterval]{findInterval()}} call rather than a per-character regular expression.

Two of the rows deserve comment. Halfwidth Katakana (U+FF65-U+FF9F) is a
sub-range of the Halfwidth and Fullwidth Forms block; because the katakana
label is the more useful one, the enclosing block appears as two rows either
side of it. And the halfwidth Hangul jamo at U+FFA0-U+FFDC are reported as
\code{"fullwidth"} rather than \code{"hangul"}, because this table follows the block
boundaries rather than the Unicode Script property; use \code{\link[=cjk_char_counts]{cjk_char_counts()}}
if you need to see exactly which code points a text contains.

All nine blocks of unified ideographs are covered, through Extension I. They
are not in alphabetical order, because Unicode allocated Extension I
(U+2EBF0) below Extension G (U+30000) rather than after Extension H, and
this table is in code point order.

The \code{script} column takes one of eight values. Six of them name a writing
system -- \code{"han"}, \code{"hiragana"}, \code{"katakana"}, \code{"hangul"}, \code{"bopomofo"},
\code{"kanbun"} -- and two are ancillary: \code{"punctuation"} for the CJK Symbols and
Punctuation block, and \code{"fullwidth"} for the width-variant forms. Only the
six writing systems are consulted by \code{\link[=cjk_detect_language]{cjk_detect_language()}}.
}
\examples{
cjk_blocks()

# the blocks that make up "han"
subset(cjk_blocks(), script == "han")
}
\seealso{
\code{\link[=cjk_script]{cjk_script()}} for the dominant script of a string,
\code{\link[=cjk_char_counts]{cjk_char_counts()}} for a per-character breakdown.
}
