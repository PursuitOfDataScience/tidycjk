% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collate.R
\name{cjk_sort}
\alias{cjk_sort}
\alias{cjk_order}
\title{Sort CJK text by pronunciation or stroke}
\usage{
cjk_sort(x, locale = NULL, decreasing = FALSE)

cjk_order(x, locale = NULL, decreasing = FALSE)
}
\arguments{
\item{x}{A character vector.}

\item{locale}{ICU locale naming the collation, e.g. \code{"zh"}. \code{NULL}, the
default, uses the session default -- which is unlikely to order Han
usefully, so name one.}

\item{decreasing}{Reverse the order.}
}
\value{
\code{cjk_sort()} a character vector the same length as \code{x};
\code{cjk_order()} an integer vector of indices.
}
\description{
\code{cjk_sort()} returns \code{x} ordered by an ICU collation; \code{cjk_order()} returns
the permutation that would do it, for reordering a data frame.
}
\details{
\code{sort()} reads \code{LC_COLLATE}, so the order it gives Han text is a property
of the session rather than of the data. Under \code{C} or \code{en_US.UTF-8} it
falls back to code point order; under \code{zh_CN.utf8} glibc supplies a pinyin
collation and the answer changes; under \code{ja_JP.utf8} it changes again.
None of those is wrong in itself, and that is the difficulty: the same
script sorts a name column differently on two machines and says nothing.

The fallback order is worth knowing, because it is not arbitrary. The
unified ideographs are laid out in KangXi radical-stroke order, so within
the base block sorting by code point sorts by radical. What it is not is
phonetic, which is what someone sorting names is after. It also stops
being radical order across blocks: every Extension A character
(U+3400-U+4DBF) sorts ahead of every base-block one, so a rare character
lands nowhere near the common characters sharing its radical.

ICU carries real collations. With \code{locale = "zh"} Han sorts by pinyin, so a
column of Chinese surnames comes out in the order a Chinese reader expects.
}
\section{Choosing the order}{
The collation is selected by the locale string, including the BCP 47
variants:
\itemize{
\item \code{"zh"} -- Mandarin pinyin.
\item \code{"zh-u-co-stroke"} -- stroke count, the convention in many indexes.
\item \code{"zh-u-co-zhuyin"} -- bopomofo order.
\item \code{"ja"} and \code{"ko"} -- Japanese and Korean.
}

A locale whose \emph{language} ICU has no collation for is an error here rather
than the silent fallback to root collation stringi would otherwise give
you, because that fallback produces a plausible-looking wrong order. An
unrecognised \emph{region} is not an error: \code{"zh-CH"}, a typo for \code{"zh-CN"},
resolves to \code{"zh"} and still sorts by pinyin, which is the right answer.
}

\section{Missing values}{
\code{NA} sorts last and is kept, so the result is always the same length as
\code{x}. \code{stringi::stri_sort()} drops missing values by default, which would
silently shorten a column.
}

\examples{
# Chinese surnames: Zhang, Wang, Li
x <- c("\u5f35", "\u738b", "\u674e")

sort(x)                        # depends on LC_COLLATE
cjk_sort(x, locale = "zh")     # pinyin: Li, Wang, Zhang -- on any machine

# stroke order instead
cjk_sort(x, locale = "zh-u-co-stroke")

# reorder a data frame
df <- data.frame(name = x)
df[cjk_order(x, locale = "zh"), , drop = FALSE]
}
\seealso{
\code{\link[=cjk_romanize]{cjk_romanize()}}, whose \code{ascii = TRUE} output gives a sort key you
can use where no ICU locale is available.
}
