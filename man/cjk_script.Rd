% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect.R
\name{cjk_script}
\alias{cjk_script}
\title{Which CJK script dominates the text?}
\usage{
cjk_script(x)
}
\arguments{
\item{x}{A character vector. Anything else is coerced with \code{\link[=as.character]{as.character()}}.}
}
\value{
A character vector the same length as \code{x}. Strings with no CJK
characters -- and \code{NA} strings, and \code{""} -- give \code{NA}.
}
\description{
\code{cjk_script()} returns the script that accounts for the most CJK characters
in each string: one of \code{"han"}, \code{"hiragana"}, \code{"katakana"}, \code{"hangul"},
\code{"bopomofo"}, \code{"kanbun"}, \code{"punctuation"} or \code{"fullwidth"}.
}
\details{
Only CJK characters vote. Latin letters, digits and whitespace are ignored
entirely, so a string of English with two ideographs in it is \code{"han"} rather
than something averaged over the whole string.

Ties are broken by first appearance in the string -- not alphabetically and
not by the session's collation -- so the result never depends on the locale.
}
\examples{
# Chinese, Japanese, Korean, then a string with no CJK at all
cjk_script(c("\u4e2d\u6587", "\u3053\u3093\u306b\u3061\u306f",
             "\uc548\ub155", "ascii"))

# mixed Han and kana: four kana outvote two ideographs
cjk_script("\u65e5\u672c\u306e\u3053\u3068\u3070")
}
\seealso{
\code{\link[=cjk_blocks]{cjk_blocks()}} for the script labels, \code{\link[=cjk_char_counts]{cjk_char_counts()}} for the
full per-character breakdown rather than just the winner.
}
