% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/width.R
\name{cjk_width}
\alias{cjk_width}
\title{Display width in terminal columns}
\usage{
cjk_width(x)
}
\arguments{
\item{x}{A character vector. Anything else is coerced with \code{as.character()}.}
}
\value{
An integer vector the same length as \code{x}. \code{NA} input gives \code{NA}; the
empty string gives \code{0}.
}
\description{
\code{cjk_width()} returns the number of columns each string occupies in a
monospaced terminal. CJK characters occupy two columns, not one, which is
the reason \code{nchar()} misaligns every console table containing CJK text.
}
\details{
Width follows Unicode Annex #11 (East Asian Width). Characters whose East
Asian Width is Wide or Fullwidth are two columns; combining marks and format
characters (general categories \code{Mn}, \code{Me}, \code{Cf}), C0 and C1 control codes,
and Hangul Jamo medial vowels and final consonants are zero; everything else
is one.

The computation is \code{stringi::stri_width()}, which reads ICU's Unicode
tables. \code{cjk_width()} exists so that the width, the padding and the
truncation in a CJK pipeline all read the same way; if width is all you
need, \code{stri_width()} is the more direct call.

East Asian Ambiguous characters -- Greek letters, some box-drawing, the
degree sign -- are one column. They render as two in a CJK-configured
terminal and one everywhere else, and no library can resolve that without
knowing the terminal.
}
\examples{
# two characters, four columns
cjk_width("\u4e2d\u6587")

# two characters, two columns
cjk_width("ab")

# nchar() cannot tell these apart; cjk_width() can
nchar(c("\u4e2d\u6587", "abcd"))
cjk_width(c("\u4e2d\u6587", "abcd"))
}
\seealso{
\code{\link[=cjk_pad]{cjk_pad()}} and \code{\link[=cjk_truncate]{cjk_truncate()}}, which lay text out by width;
\code{stringi::stri_width()} for the underlying computation.
}
