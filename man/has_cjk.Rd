% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect.R
\name{has_cjk}
\alias{has_cjk}
\title{Does the text contain CJK characters?}
\usage{
has_cjk(x)
}
\arguments{
\item{x}{A character vector. Anything else is coerced with \code{as.character()}.}
}
\value{
A logical vector the same length as \code{x}. \code{NA} input gives \code{NA}; the
empty string gives \code{FALSE}.
}
\description{
\code{has_cjk()} reports, for each element, whether the string contains at least
one character from a CJK Unicode block.
}
\details{
"CJK" here means any block listed by \code{\link[=cjk_blocks]{cjk_blocks()}}, which includes CJK
punctuation and the halfwidth and fullwidth forms as well as the ideographs
and the phonetic scripts. That is deliberate -- a column typed with a CJK
input method carries the punctuation too -- but it does mean that a string
of nothing but ideographic full stops (U+3002) is \code{TRUE}. Use \code{\link[=cjk_script]{cjk_script()}}
when you need to know \emph{which} kind of CJK you have.
}
\examples{
# U+4E2D U+6587, "Chinese writing"
has_cjk(c("\u4e2d\u6587", "plain ASCII", NA))

# the ideographic full stop U+3002 counts, by design
has_cjk("\u3002")
}
\seealso{
\code{\link[=cjk_ratio]{cjk_ratio()}} for how much of the text is CJK, \code{\link[=cjk_script]{cjk_script()}} for
which script it is.
}
