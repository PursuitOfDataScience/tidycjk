% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect.R
\name{cjk_detect_language}
\alias{cjk_detect_language}
\title{Which language is the text written in?}
\usage{
cjk_detect_language(x, han_only = NA_character_)
}
\arguments{
\item{x}{A character vector. Anything else is coerced with \code{as.character()}.}

\item{han_only}{What to return for a string written in Han characters only.
Defaults to \code{NA_character_}, meaning "undecidable". Set it to \code{"chinese"}
to opt into the guess.}
}
\value{
A character vector the same length as \code{x}, holding \code{"japanese"},
\code{"korean"}, \code{"chinese"}, the value of \code{han_only}, or \code{NA}.
}
\description{
\code{cjk_detect_language()} infers the language of each string from the scripts
it contains, and returns \code{NA} when the scripts do not settle the question.
}
\details{
The rules are applied in order:

\enumerate{
\item Hiragana or katakana present: \code{"japanese"}. Only Japanese uses kana.
\item Kanbun annotation marks present: \code{"japanese"}. They exist to make
Classical Chinese readable as Japanese.
\item Hangul present: \code{"korean"}.
\item Bopomofo present: \code{"chinese"}. Bopomofo annotates Mandarin.
\item Han characters and nothing else from the list above: \strong{\code{NA}} by default.
\item No CJK writing system at all: \code{NA}.
}

Rule 5 is the point of the function. Japanese written without kana -- a
headline, a shop sign, a personal name, a compound such as U+6771 U+4EAC
U+90FD (Tokyo Metropolis) -- is not distinguishable from Chinese by script
alone, because both are writing the same Han characters. Any package that
answers \code{"chinese"} there is guessing, and it will be wrong on Japanese
input in a way the caller cannot detect. \code{tidycjk} returns \code{NA} instead.

If your corpus is known to be Chinese and you want the guess anyway, ask for
it explicitly with \code{han_only = "chinese"}. Making it an argument keeps the
assumption in the script, where a reader of the analysis can see it.

CJK punctuation and fullwidth forms are ignored here, even though
\code{\link[=has_cjk]{has_cjk()}} counts them, because all three languages share them.
}
\examples{
# kana settles it; hangul settles it
cjk_detect_language(c("\u3053\u3093\u306b\u3061\u306f", "\uc548\ub155"))

# U+6771 U+4EAC U+90FD is Tokyo Metropolis: Japanese, written without kana,
# and therefore indistinguishable from Chinese. The honest answer is NA.
cjk_detect_language("\u6771\u4eac\u90fd")

# opt in to the guess when you know the corpus is Chinese
cjk_detect_language("\u6771\u4eac\u90fd", han_only = "chinese")
}
\seealso{
\code{\link[=cjk_script]{cjk_script()}}, which reports what is actually there rather than
inferring from it.
}
