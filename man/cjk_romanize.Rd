% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transliterate.R
\name{cjk_romanize}
\alias{cjk_romanize}
\title{Romanise CJK text}
\usage{
cjk_romanize(x, ascii = FALSE)
}
\arguments{
\item{x}{A character vector.}

\item{ascii}{If \code{TRUE}, strip diacritics so the result is plain ASCII:
pinyin tone marks are removed, so that \code{wo} with a caron becomes plain
\code{wo}. Defaults to \code{FALSE},
which keeps them.}
}
\value{
A character vector the same length as \code{x}. \code{NA} gives \code{NA}.
}
\description{
Transliterates Han, kana and Hangul into the Latin alphabet: pinyin for
Chinese, romaji for Japanese kana, Revised-Romanisation-style output for
Korean.
}
\details{
The mapping is ICU's \code{Any-Latin}, which handles all three scripts in one
pass, so mixed text does not need splitting first. Text that is already
Latin is left alone.
}
\section{What this is not}{
Romanisation is not a function of the code point alone, and ICU treats it
as though it were. Three consequences worth knowing before you trust the
output:
\itemize{
\item \strong{Han is always read as Chinese.} ICU routes every Han character
through pinyin, whatever language surrounds it, so Japanese kanji come
back with Chinese readings: U+65E5 U+672C U+8A9E ("Japanese language")
romanises to \verb{ri ben yu}, not \code{nihongo}. This is not a near miss to be
cleaned up afterwards -- it is the wrong language. Do not use
\code{cjk_romanize()} on Japanese kanji.
\item \strong{No word spacing.} ICU inserts a space between Han syllables but not
between scripts, so Han followed immediately by kana romanises to a run
with no boundary where the script changes. Segment first with
\code{\link[=cjk_segment]{cjk_segment()}} if you need words.
\item \strong{Kana particles are spelled, not pronounced.} The Japanese topic
particle U+306F romanises to \code{ha}, which is how it is written and not
how it is said.
}

For Japanese specifically, a morphological analyser that knows the reading
-- \href{https://CRAN.R-project.org/package=gibasa}{gibasa}, which binds MeCab
-- is the right tool. This function is for Chinese, for kana, and for
getting a sortable ASCII key out of a CJK column.
}

\examples{
# U+4E2D U+6587, "Chinese writing"
cjk_romanize("\u4e2d\u6587")            # pinyin, with tone marks
cjk_romanize("\u4e2d\u6587", ascii = TRUE)

# Han is read as Chinese even in Japanese text -- see Details
cjk_romanize("\u65e5\u672c\u8a9e")

# kana and Hangul go through the same call
cjk_romanize(c("\u3053\u3093\u306b\u3061\u306f", "\uc548\ub155"))
}
\seealso{
\code{\link[=cjk_simplify]{cjk_simplify()}} for Han script conversion, \code{\link[=cjk_segment]{cjk_segment()}} for
word boundaries.
}
