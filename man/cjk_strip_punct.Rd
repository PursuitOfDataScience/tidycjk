% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nlp.R
\name{cjk_strip_punct}
\alias{cjk_strip_punct}
\title{Remove punctuation from CJK text}
\usage{
cjk_strip_punct(x, replacement = " ", symbols = FALSE)
}
\arguments{
\item{x}{A character vector. Anything else is coerced with
\code{\link[=as.character]{as.character()}}. That coercion is R's, not this package's, so a
numeric vector is measured as R chooses to write it -- which moves
with \code{options(scipen)} and \code{options(OutDec)}, and can therefore
differ between sessions. Convert deliberately if you mean to
measure numbers; these verbs are for text.}

\item{replacement}{A single string to put in place of each removed
character. Defaults to \code{" "}; see above for why it is not \code{""}.}

\item{symbols}{Also remove General_Category \code{S}: the fullwidth tilde
U+FF5E, currency signs including U+FFE5, and mathematical operators.
Defaults to \code{FALSE}, because a currency sign is often content.}
}
\value{
A character vector the same length as \code{x}. \code{NA} gives \code{NA}, and a
leading byte-order mark is preserved.
}
\description{
\code{cjk_strip_punct()} removes punctuation using Unicode's own category
rather than a POSIX class, so the same call removes the same characters
on every machine. It is the step before \code{\link[=cjk_ngrams]{cjk_ngrams()}} or
\code{\link[=cjk_segment]{cjk_segment()}} that stops punctuation becoming part of a token.
}
\details{
The usual spelling, \code{gsub("[[:punct:]]", "", x)}, is unreliable on CJK in
two separate ways. R's default engine resolves \verb{[:punct:]} through the C
library, so under \code{LC_ALL=C} it removes no CJK punctuation and under a
UTF-8 locale it removes all of it -- the same script gives two answers on
two machines. Passing \code{perl = TRUE} does not fix it but hides it: PCRE's
POSIX classes are ASCII-only unless \verb{(*UCP)} is set, so that spelling
silently removes nothing from Chinese or Japanese in any locale at all.

This removes Unicode General_Category \code{P}, which is the ideographic full
stop U+3002, the ideographic comma U+3001, the fullwidth comma, question
mark and exclamation mark, the corner and fullwidth brackets, and the
katakana middle dot U+30FB that separates the parts of a transliterated
name -- along with ASCII punctuation, so mixed text needs only one pass.
}
\section{What it deliberately keeps}{
U+30FC, the katakana-hiragana prolonged sound mark, is a \emph{modifier
letter} and not punctuation. It looks like a dash and is not one: it
carries the long vowel in most Japanese loanwords, so removing it turns
the words for coffee and ramen into something else. Testing the category
keeps it; any rule phrased about dashes does not.

The ideographic space U+3000 is whitespace rather than punctuation and is
left alone too. \code{\link[=cjk_segment]{cjk_segment()}} and \code{\link[=cjk_ngrams]{cjk_ngrams()}} already ignore
whitespace, so there is nothing to do about it here.
}

\section{Why the default replaces rather than deletes}{
\code{replacement} defaults to a space, not \code{""}. Deleting a full stop closes
the gap it left, and the two characters that were on either side of it
become adjacent: \code{cjk_ngrams()} then reports a bigram spanning a sentence
boundary, a "word" that was never written. A space keeps the boundary,
and every verb here that walks a string already declines to cross
whitespace. Pass \code{replacement = ""} when you want the characters closed
up -- for a display string rather than for tokenising.
}

\examples{
x <- "\u4ed6\u8aaa\uff08\u4eca\u5929\uff09\u3002\u771f\u597d"
cjk_strip_punct(x)
cjk_strip_punct(x, replacement = "")

# the prolonged sound mark is a letter, not a dash, and survives
cjk_strip_punct("\u30b3\u30fc\u30d2\u30fc\u3001\u30e9\u30fc\u30e1\u30f3")

# a space keeps n-grams from spanning the full stop
cjk_ngrams(cjk_strip_punct("\u597d\u3002\u5929"))
cjk_ngrams(cjk_strip_punct("\u597d\u3002\u5929", replacement = ""))
}
\seealso{
\code{\link[=cjk_ngrams]{cjk_ngrams()}} and \code{\link[=cjk_segment]{cjk_segment()}}, the verbs this feeds;
\code{\link[=cjk_normalize]{cjk_normalize()}} for folding width and compatibility variants.
}
